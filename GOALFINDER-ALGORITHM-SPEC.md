# GoalFinder 推荐引擎与志愿诊断中心 — 算法说明书 (v1.0)

> **技术基础**: 概率统计 / 机器学习回归 / 领域专家规则引擎 (Rule Engine)

---

## 1. 投档推荐核心算法：位次偏移法 (Ranking Displacement)

高考填报中，“位次”比“分数”更具参考价值。系统核心采用“位次弹性区间”算法。

### 1.1 基础模型
- **位次位移量 (ΔR)**：`ΔR = Target_Rank_Avg(Yr-1, Yr-2) - User_Rank`
- **录取概率函数 (P)**：
  $$P = \frac{1}{1 + e^{-k(ΔR + b)}}$$
  其中 $k$ 为斜率因子（反映该校历年位次波动率），$b$ 为偏差校正（反映今年扩招/减招影响）。

### 1.2 冲、稳、保区间定义 (Strategic Gradients)

| 等级 | 策略名词 | 位次偏移区间 (ΔR) | 推荐逻辑 |
| :--- | :--- | :--- | :--- |
| **冲 (Deep Reach)** | 极少数奇迹 | ΔR ∈ [-5%, -1%] | 挖掘往年位次偶发性下跌的学校，适合追求更高平台的用户。 |
| **稳 (Stable)** | 门当户对 | ΔR ∈ [0, +5%] | 锁定录取位次极其稳定、就业口碑好的核心院校。 |
| **保 (Safe)** | 底封保障 | ΔR > +15% | 确保 0 风险录取，作为志愿表的最后 2-3 个保底选项。 |

---

## 2. 志愿表诊断系统 (Diagnosis Logic)

系统通过对用户拟定的“志愿表”进行全扫描，发现逻辑漏洞。

### 2.1 梯度合理性检查 (Gradient Audit)
- **漏斗法则**：检查志愿序列 $V_1, V_2, ... V_n$ 的历史位次均值是否满足递增关系（即难度递减）。
- **报警项**：若 $V_3$ 的录取难度高于 $V_1$，系统标记为“无效梯度”，警告用户 $V_1, V_2$ 若未录取，$V_3$ 极大概率也无法投档。

### 2.2 调剂风险评估 (Major Adjustment Risk)
- **冲突检测**：检查分数是否处于该校专业录取的“边缘地带”。
- **风险提示**：若用户分数仅略高于投档线，且未勾选“服从专业调剂”，系统强制标记为红色“退档危急”。

### 2.3 分数节约度分析 (Score Efficiency)
- **浪费评估**：若用户全省排名第 5000，却填报了往年位次在 20000 左右的学校作为第一志愿，系统提醒“高分低就”浪费程度。

---

## 3. 专业/职业偏好匹配 (AI Major Recommendation)

采用语义匹配算法而非简单的关键词匹配。

- **Embeddings 空间匹配**：利用 Gemini 对专业描述、就业领域、培养目标进行向量化。
- **用户画像映射**：
    - 输入：`"我性格安静，喜欢逻辑推理，不喜欢在户外工作"`。
    - 输出：AI 计算与“计算机科学”、“数学”、“审计学”的余弦相似度，并推荐对应位次的学校专业。

---

## 4. 特殊规则引擎 (Expert Rule Layers)

针对各省份差异化的硬性规定：
- **选科硬性过滤 (Prerequisite Check)**：自动剔除物理/历史选科不符的院校。
- **单科最低分限制**：部分专业要求外语 >120 或 数学 >110，AI 自动扫描抓取的简章并校验。
- **体检/性别限制**：针对军校、特殊行业院校的强限制自动过滤。

---

## 5. 算法优化：自反馈学习 (Feedback Loop)

在每一波数据入库后，AI 自动重算过去 3 年位次的**标准差 (Standard Deviation)**。
- 若标准差大（位次暴涨暴跌）：算法会自动调大“稳”的位次缓冲区间。
- 若标准差小（位次极其稳定）：算法会缩小缓冲区间，提供更精准的预测建议。
